<x-layouts.admin title="L. C. Entries | Forest Inventory" heading="L. C. Entry" subheading="Division L. C. entries"><div class="space-y-5">@if(session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-800">{{ session('status') }}</div>@endif<div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><input class="form-input min-h-9 py-1.5 sm:max-w-sm" placeholder="Search L. C. entries..." data-lc-entry-search><a class="primary-button whitespace-nowrap sm:ml-auto" href="{{ route('division.lc-entries.create') }}">Add L. C. Entry</a></div><div class="overflow-x-auto rounded-lg border border-emerald-100 bg-white"><table class="data-table compact-data-table min-w-[60rem]"><thead><tr><th>Sr. No.</th><th>Scheme</th><th>Class</th><th>Date</th><th>Amount</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($entries as $entry)<tr data-lc-entry-row><td>{{ $entry->serial_number }}</td><td>{{ $entry->scheme }}</td><td>{{ $entry->class }}</td><td>{{ $entry->entry_date->format('d-m-Y') }}</td><td>{{ number_format((float)$entry->amount,2) }}</td><td><div class="flex gap-1"><a class="secondary-button min-h-7 px-1.5 py-0.5 text-[10px]" href="{{ route('division.lc-entries.edit',$entry) }}">Edit</a><form method="POST" action="{{ route('division.lc-entries.destroy',$entry) }}" onsubmit="return confirm('Delete this L. C. entry?')">@csrf @method('DELETE')<button class="secondary-button min-h-7 border-red-200 bg-red-50 px-1.5 py-0.5 text-[10px] text-red-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center">No L. C. entries available.</td></tr>@endforelse</tbody></table></div>@if($entries->hasPages())<div class="border-t border-emerald-100 px-5 py-4">{{ $entries->links() }}</div>@endif</div></x-layouts.admin>
