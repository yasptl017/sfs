<x-layouts.admin title="Allotment Adjustments | Forest Inventory" heading="Allotment Adjustment" subheading="Transfer an allotment from one budget code to another"><div class="space-y-5">@if(session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-800">{{ session('status') }}</div>@endif<div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><input class="form-input min-h-9 py-1.5 sm:max-w-sm" placeholder="Search adjustment details..." data-adjustment-search><a class="primary-button whitespace-nowrap sm:ml-auto" href="{{ route('division.allotment-adjustments.create') }}">Add Adjustment</a></div><div class="overflow-x-auto rounded-lg border border-emerald-100 bg-white"><table class="data-table compact-data-table min-w-[75rem]"><thead><tr><th>Date</th><th>From Budget Code</th><th>To Budget Code</th><th>Amount Transferred</th><th>Remark</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($entries as $entry)<tr data-adjustment-row><td>{{ $entry->created_at->format('d-m-Y') }}</td><td>{{ $entry->data['from']['budget_code'] ?? '-' }}</td><td>{{ $entry->data['to']['budget_code'] ?? '-' }}</td><td>{{ number_format((float)$entry->amount,2) }}</td><td>{{ $entry->remark ?? '-' }}</td><td><div class="flex gap-1"><a class="secondary-button min-h-7 px-1.5 py-0.5 text-[10px]" href="{{ route('division.allotment-adjustments.edit',$entry) }}">Edit</a><form method="POST" action="{{ route('division.allotment-adjustments.destroy',$entry) }}" onsubmit="return confirm('Delete this adjustment?')">@csrf @method('DELETE')<button class="secondary-button min-h-7 border-red-200 bg-red-50 px-1.5 py-0.5 text-[10px] text-red-700">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center">No allotment adjustments available.</td></tr>@endforelse</tbody></table></div>@if($entries->hasPages())<div class="border-t border-emerald-100 px-5 py-4">{{ $entries->links() }}</div>@endif</div></x-layouts.admin>
